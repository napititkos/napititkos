# Másold .env.local néven, és írj bele egy erős jelszót.
# Éles környezetben (Vercel) ezt a Project Settings > Environment Variables alatt add meg.
ADMIN_PASSWORD=valami-eros-jelszo

# A KV_REST_API_URL és KV_REST_API_TOKEN automatikusan bekerül,
# amint a Vercel projekthez hozzáadod a Vercel KV storage-ot.
